func main(): none {
    @a: list |int| = [1,4,5,7];
    @b: list |bool| = [true, false, false];

    print("Int list");
    print("----------------");

    print("length of list = " + |as string| list_len(@a));
    
    @idx: int = 0;
    
    @len: int = list_len(@a);
    print(@len);
    
    print("let's iterate through the list"); 
    //for @idx in 0 ... < @len {
    for @idx in 0 ... < 4 {
        //print(@a[@idx]); unimplemented
        //print(@a[@idx]);
    }
    

}