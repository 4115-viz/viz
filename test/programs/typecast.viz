func main(): none {
    
    @ok: float = 0.0;

    // this doesn't work
    // @a : float = | as float | 100;

    // this gives cannot cast i32 to double
    // print(| as float | 100);

    | as float| 1;

    print(| as string| @ok);

    // this gives cannot cast double to i32
    //print (| as int| 1.0);
}