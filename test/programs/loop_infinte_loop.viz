func main(): none { // this fails
//func main(): int { // this runs
    
    // map this infinite_loop to while (true)
    // this will be parsed as a while loop where
    // true is the condition
    infinite_loop {
        print("let do this forever");
    }
    //return 0;
}